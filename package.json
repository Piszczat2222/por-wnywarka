{
  "name": "altpik",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/cache-product-images.mjs && node scripts/fetch-listicle-thumbnails.mjs && node scripts/generate-og.mjs && astro build && node scripts/patch-sitemap-lastmod.mjs && node scripts/indexnow.mjs",
    "thumbnails": "node scripts/fetch-listicle-thumbnails.mjs",
    "preview": "astro preview",
    "deploy": "npm run build && wrangler pages deploy dist"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "@tailwindcss/typography": "^0.5.20",
    "@tailwindcss/vite": "^4.3.2",
    "astro": "^7.0.0",
    "tailwindcss": "^4.3.2",
    "wrangler": "^4.106.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@astrojs/rss": "^4.0.19",
    "@resvg/resvg-js": "^2.6.2"
  }
}
